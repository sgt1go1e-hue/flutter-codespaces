# ベータ版ビルド用の環境変数。`npm run build:beta` (vite build --mode beta) 実行時に
# 自動で読み込まれる。永久版の通常ビルド(`npm run build`)には影響しない。
VITE_BETA_LOCK=true
# ベータ公開の期限日(JSTの0時で終了)。テスターごとの経過日数ではなく、
# 全員共通のこの日付で判定する。延長するときはこの日付だけ書き換えて
# ビルドし直す(環境変数はビルド時に埋め込まれるため再デプロイが必要)。
VITE_BETA_END_DATE=2026-09-15
